# Zebra Robotics HintGenerator

Core_runtime_loop = This repository contains the backend runtime loop for the Zebra Robotics AI Tutor — a Service that receives student queries and optional image/C++ file inputs, processes them through GPT-4o, stores conversation history in Cloud SQL, and returns a tutoring response.
